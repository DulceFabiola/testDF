import React from "react";
import Camioneta from "./Camioneta";

const Agencia = () => {
  return (
    <div>
      <Camioneta modelo={`2021`} marca={`Volkswagen`} agencia={`México`} />
    </div>
  );
};

export default Agencia;
